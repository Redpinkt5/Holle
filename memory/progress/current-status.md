# 当前状态

- **版本**：v0.3.3（已推送 tag），本地另有未推送的播放兜底修复 + AI 记忆功能
- **主要完成项**：
  - `/ai <apikey>` 自动识别 AI 供应商，`/model <模型名>` 切换模型
  - TUI 与 Pet 共享 `~/.holle_music/settings.json` 中的 AI 配置
  - TUI 支持 AI tool calling 控制播放器
  - Pet 输入气泡支持 `Ctrl+V`/`Ctrl+C`/`Ctrl+A` 与右键粘贴
  - 修复 DeepSeek tool-call 历史格式错误
  - 修复 AI 工具参数 JSON 字符串未解析导致本地/联网搜索失败的问题
  - 强化系统提示，要求 AI 在找到歌曲后必须调用 `play_song` 工具实际播放
  - 增加兜底：AI 只搜索不播放时，若用户有播放意图则自动播放最佳匹配结果（本地 commit `34cb261`，未推送）
  - 新增 `ai_memory.py`：STM + LTM 持久化记忆，自动注入 prompt，TUI 与 Pet 聊天时记录对话与决策（本地未推送）
  - 为非 tool-calling 服务增加本地播放兜底：检测到播放意图后自动搜索并播放（本地未推送）
  - 修复 Pet 独立运行时播放/音量/模式命令只发 IPC 不实际执行的问题（本地未推送）
  - 新增 `play_artist` 工具：播放某位歌手所有本地歌曲并加载为当前列表（本地未推送）
  - AI 回复与正在播放封面气泡支持追加合并，避免封面覆盖文字回复；已修复因 `append` 导致的重复打印死循环（本地未推送）
- **测试**：93/93 通过
- **待观察**：合并气泡无死循环、整歌手列表播放测试通过后，再 bump v0.3.4 并推送
